--- frontend/src/components/NoteList.tsx
+++ frontend/src/components/NoteList.tsx
@@ -1429,6 +1429,11 @@
     }
   }, [notes, isPinnedOnly, isFavoriteOnly, isTrashedOnly, isArchivedOnly, search, tagId, dateFilter, sortPref, state.notebooks]);

+  const handleCardRef = useCallback((id: string, el: HTMLDivElement | null) => {
+    if (el) noteCardRefs.current.set(id, el);
+    else noteCardRefs.current.delete(id);
+  }, []);
+
   // 快捷键支持：Delete 键删除当前选中项
   useEffect(() => {
     const handler = async (e: KeyboardEvent) => {
