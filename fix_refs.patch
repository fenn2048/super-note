--- frontend/src/components/NoteList.tsx
+++ frontend/src/components/NoteList.tsx
@@ -1125,10 +1125,7 @@
           {visibleNotes.map((note) => (
             <NoteCard
               key={note.id}
-              cardRef={(el) => handleCardRef(note.id, el) /* FIXED:
-                if (el) noteCardRefs?.current.set(note.id, el);
-                else noteCardRefs?.current.delete(note.id);
-              }}
+              cardRef={(el) => handleCardRef(note.id, el)}
               note={note}
               isActive={activeNoteId === note.id}
               isContextTarget={menuState.isOpen && menuState.targetId === note.id}
@@ -3105,10 +3102,7 @@
                   sortedNotes.map((note) => (
                     <NoteCard
                       key={note.id}
-                      cardRef={(el) => handleCardRef(note.id, el) /* FIXED:
-                        if (el) noteCardRefs.current.set(note.id, el);
-                        else noteCardRefs.current.delete(note.id);
-                      }}
+                      cardRef={(el) => handleCardRef(note.id, el)}
                       note={note}
                       isActive={state.activeNote?.id === note.id}
                       isContextTarget={menu.isOpen && menu.targetId === note.id}
@@ -3265,10 +3259,7 @@
                   {sortedNotes.map((note) => (
                     <NoteCard
                       key={note.id}
-                      cardRef={(el) => handleCardRef(note.id, el) /* FIXED:
-                        if (el) noteCardRefs.current.set(note.id, el);
-                        else noteCardRefs.current.delete(note.id);
-                      }}
+                      cardRef={(el) => handleCardRef(note.id, el)}
                       note={note}
                       isActive={state.activeNote?.id === note.id}
                       isContextTarget={menu.isOpen && menu.targetId === note.id}
