services:
  super-note:
    image: {{IMAGE}}
    container_name: super-note
    restart: always
    network_mode: host
    volumes:
      - ${DATA_PATH}:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/super-note.db
      - PORT=3001
      - TZ=${TZ}
